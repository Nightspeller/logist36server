<!DOCTYPE html>
<html>
<head>
    <title>Альянс Логистов Воронежа</title>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8">
    <!-- Bootstrap -->
    <link href="css/bootstrap.min.css" rel="stylesheet" media="screen">
    <link href="css/style.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="js/html5shiv.js"></script>
    <script src="js/respond.min.js"></script>
    <![endif]-->
</head>
<body>
<div class="container">
    <section class="row">
        <div class="col-sm-3" id="globalLogoAndSideNav">{{{header}}}</div>
        <div class="col-sm-9">
            <nav class="row topNav" id="topNav">{{{topNav}}}</nav>
            <!--Main content goes here-->

            <article class="panel panel-primary mainContent">
                <div class="panel-heading">
                    <h3 class="panel-title">{{{title}}}</h3>
                </div>
                <div class="panel-body">
                    <p><strong>Полное название организации:</strong> {{{title}}}</p>
                    <img class="img-responsive pull-right" src="img/partners/{{{logo}}}">
                    <p><strong>Год основания:</strong> {{{year}}}</p>
                    <p><strong>Адрес юридический:</strong> {{{urAddress}}}</p>
                    <p><strong>Адрес фактический:</strong> {{{factAddress}}}</p>
                    <p><strong>Тел./факс:</strong> {{{phone}}}</p>
                    <p><strong>Е-mail:</strong> {{{email}}}</p>
                    <p><strong>Сайт компании:</strong> <a href="{{{website}}}">{{{website}}}</a></p>
                    <p><strong>Руководитель:</strong> {{{director}}}</p>
                    <p><strong>Бухгалтер:</strong> {{{accountant}}}</p>
                    <p><strong>ИНН:</strong> {{{inn}}}</p>
                    <p><strong>ОКВЭД:</strong> {{{okved}}}</p>
                    <p><strong>Описание деятельности компании:</strong> {{{fullDescription}}}</p>
                    <p><strong>Направление сотрудничества:</strong> {{{partnership}}}</p>
                </div>
                <div class="panel-footer">Если необходимо, тут можно кратко разместить подытоживающее предложение.</div>
            </article>

        </div>
    </section>
    {{{footer}}}
</div>
<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="js/jquery-1.10.2.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="js/bootstrap.min.js"></script>
<script src="js/main.js"></script>
</body>
</html>